/**
 * Defund the Dakota Access Pipeline
 * defunddapl.org
 * @flow
 */

import { AppRegistry } from 'react-native';
import App from './src/App';

AppRegistry.registerComponent('defundDAPL', () => App);
